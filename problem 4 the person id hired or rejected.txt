#include<iostream>
#include<string>
using namespace std;
struct stinfo
{
	int age;
	bool havedriverlicence;
};
stinfo readperson()
{
	stinfo info;
	cout << "please entre your age";
	cin >> info.age;
	cout << "do you have driver licenc?" << endl;
	cin >> info.havedriverlicence;
	return info;
}
bool isaccepted(stinfo info)
{
	return(info.age > 21 && info.havedriverlicence);
}
void printperson(stinfo info)
{
	if (isaccepted(info))
		cout << "is hired";
	else
		cout << "is rejected";
}
int main()
{

	printperson(readperson());
	return 0;
}