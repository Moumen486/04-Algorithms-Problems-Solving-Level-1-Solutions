#include<iostream>
#include<string>
#include<cmath>
using namespace std;
const float PI = 3.14;
void readarea(float& a,float& b,float& c)
{
	cout << "please entre the the a of the triangle: " << endl;
	cin>> a;
	cout << "please entre the b of the traingle: " << endl;
	cin >> b;
	cout << "please entre the c of the traingle: " << endl;
	cin >> c;

}
float calculatearea(float a,float b,float c)
{
	float p = (a + b + c) / 2;
	float area = PI * pow((a * b * c) / (4 * sqrt(p * (p - a) * (p - b) * (p - c))), 2);
	return area;
}
void printarea(float area)
{
	cout << "the area is: " <<area<< endl;
}
int main()
{
	float a,b,c;
	readarea(a,b,c);
	printarea(calculatearea(a,b,c));
	return 0;
}