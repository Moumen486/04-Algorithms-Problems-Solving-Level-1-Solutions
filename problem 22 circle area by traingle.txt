#include<iostream>
#include<string>
#include<cmath>
using namespace std;
const float PI = 3.14;
void readarea(float& a,float& b)
{
	cout << "please entre the the a of the triangle: " << endl;
	cin>> a;
	cout << "please entre the b of the traingle: " << endl;
	cin >> b;
}
float calculatearea(float a,float b)
{
	float area = ((2 * a - b) / (2 * a + b)) * (pow(b, 2) / 4) * PI;
	return area;
}
void printarea(float area)
{
	cout << "the area is: " <<area<< endl;
}
int main()
{
	float a,b;
	readarea(a,b);
	printarea(calculatearea(a,b));
	return 0;
}