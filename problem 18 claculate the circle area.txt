#include<iostream>
#include<string>
#include<cmath>
using namespace std;
const float PI = 3.14;
void readarea(float& r)
{
	cout << "please entre the r: " << endl;
	cin>> r;
}
float calculatearea(float r)
{
	float area = PI * pow(r, 2);
	return area;
}
void printarea(float area)
{
	cout << "the area is: " <<area<< endl;
}
int main()
{
	float r;
	readarea(r);
	printarea(calculatearea(r));
	return 0;
}