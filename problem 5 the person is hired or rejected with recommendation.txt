#include<iostream>
#include<string>
using namespace std;
struct strinfo
{
	int age;
	bool havedriverlicence;
	bool hasrcommendation;
};
strinfo readperson()
{
	strinfo info;
	cout << "please entre your age" << endl;
	cin>> info.age;
	cout << "do you have driverlicence ?" << endl;
	cin >> info.havedriverlicence;
	cout << "do you have rcommendation" << endl;
	cin >> info.hasrcommendation;
		return info;
}
bool isaccepted(strinfo info)
{
	if (info.hasrcommendation)
		return true;
	else
	return(info.age > 21 && info.havedriverlicence);
}
void printperson(strinfo info)
{
	if (isaccepted (info))
		cout << "he is hired" << endl;
	else
		cout << "he is rejected" << endl;
}
int main()
{
	printperson(readperson());
	return 0;
}