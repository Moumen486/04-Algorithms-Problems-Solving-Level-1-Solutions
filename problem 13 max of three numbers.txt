#include<iostream>
#include<string>
using namespace std;
void readmark(int& a, int& b,int& c)
{
	cout << "please entre your first mark" << endl;
	cin >> a;
	cout << "please entre your second mark" << endl;
	cin >> b;
	cout << "plase entre the third number" << endl;
	cin >> c;
}
int themax(int a, int b, int c)
{
	if (a > b)
		if (a > c)
			return a;
		else
			return c;
	else
		if (b > c)
			return b;
		else
			return c;
}
void printmark(int max)
{
	cout << "th max is: " << max << endl;
}
int main()
{
	int a,b,c;
	readmark(a,b,c);
		printmark(themax(a,b,c));
		return 0;
}